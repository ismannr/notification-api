package controllers

import (
	"github.com/gorilla/mux"
)

func NotificationController(r *mux.Router) {
	//r.HandleFunc("/send", services.SendNotification).Methods("POST")
	//r.HandleFunc("/send-bulk", services.SendBulkNotification).Methods("POST")
}
